extends ./layouts/mainLayout.pug

block content
  section.py-5.text-center.container.backgroundElements
        .row.py-lg-5
          .col-lg-6.col-md-8.mx-auto
            h1.fw-light Proyecto #{info.title}
            img.img-fluid.mb-3(src=info.landing alt='Not Found' )
            p.lead.text-muted=info.info_body
            p
              a.btn.btn-sm.btn-secondary.my-2.me-2(href="/detalles") Volver
              a.btn.btn-sm.btn-primary.my-2.me-2(href=info.url target="_blank") Ir al sitio web
        .album.py-5
          .container.backgroundElements
          - const chunk = (arr, size) => {
          -   return arr.reduce((acc, _, i) => {
          -     if (i % size) {
          -       acc[acc.length - 1].push(arr[i]);
          -     } else {
          -       acc.push([arr[i]]);
          -     }
          -     return acc;
          -   }, []);
          - };
          - const rows = chunk(items, 3);
          each row in rows
            .row.row-cols-1.row-cols-sm-2.row-cols-sm-3.g-3
              each item in row
                .col
                  .card.shadow-sm
                    img.card-img-top(src=item.img, alt='Card image')
                    .card-body
                      p.card-text= item.text
                      .d-flex.justify-content-between.align-items-center
                        .btn-group
                          a.btn.btn-sm.btn-outline-secondary(href=`/detalles/item/${item.id}`) Ver mas